# REPRODUCE — F_presentation

*1 artifacts staged from the artifact store into this directory. 1 have recoverable generating code in lineage.*

**Toolchain(s):** python (1)

**Files & provenance:**

| file | type | class | env | code_len |
|---|---|---|---|---|
| psoriasis_meta_analysis_lab_presentation.pptx | vnd.openxmlformats-officedocument.presentationml.presentation | code_recoverable | python | 5840 |

**To regenerate a file:** fetch its generating code from artifact lineage (`host.lineage[version_id]['code']`), run it in the named conda env against the staged inputs. The version_id for each file is in `../../recoverability_audit_full.csv` (match on filename).